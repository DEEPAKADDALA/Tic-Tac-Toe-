# Tic Tac Toe (Spring Boot)

Simple Tic Tac Toe game with a Java Spring Boot backend and a static UI.

Run locally:

```powershell
cd "C:/Users/pc/OneDrive/Desktop/tic tac toe/backend"
mvn spring-boot:run
```

Then open http://localhost:8080 in your browser.
